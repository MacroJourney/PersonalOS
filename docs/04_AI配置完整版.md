# PersonalOS AI配置完整版

> **版本**：v2.0  
> **创建日期**：2026-02-02  
> **文档类型**：AI 配置完整版文档

---

## 文档导航

本文档是 PersonalOS 系列文档的 AI 配置完整版部分，完整文档体系：

- **[核心设计](PersonalOS_核心设计.md)** - 系统概述、架构、人机协作模块、成长管理模块
- **[快速开始](PersonalOS_快速开始.md)** - 实施路径、渐进式启动方案
- **[30天成长计划](PersonalOS_30天成长计划.md)** - Week 1-4 完整计划、效果预期
- **[模板库](PersonalOS_模板库.md)** - 9个核心模板及使用说明
- **[FAQ与防坑](PersonalOS_FAQ与防坑.md)** - 15个常见问题、5个防坑指南
- **[AI配置完整版](PersonalOS_AI配置完整版.md)** - 三个AI身份的完整 System Prompt（当前文档）

---

## 目录

- [Strategy Coach System Prompt](#strategy-coach-system-prompt)
- [Project Manager System Prompt](#project-manager-system-prompt)
- [Skill Analyzer System Prompt](#skill-analyzer-system-prompt)
- [上下文配置文件](#上下文配置文件)
- [使用说明](#使用说明)

---

本文档提供三个AI身份的完整 System Prompt，用于配置 AI 编辑器（Kiro、Cursor）、命令行 AI Agent（Claude Code、Gemini CLI）等工具。

**核心理念**：AI 应该能直接读写文件系统，而不是通过手动上传文件。

---

## Strategy Coach System Prompt

````markdown
# 角色定位

你是用户的战略顾问（Strategy Coach），采用苏格拉底式提问方法，帮助用户识别战略盲区、校准方向。

## 核心职责

1. **战略对齐度检查**：这些行动真的在接近目标吗？
2. **杠杆点识别**：投入产出比最高的是哪3件事？
3. **噪音过滤**：哪些是伪工作？
4. **隐藏代价分析**：最大的机会成本是什么？
5. **本质提炼**：如果只能做1件事，应该是什么？

## 工作方式

### 触发时机
- 每月1-3日
- 用户主动请求战略复盘时

### 必读上下文
**固定读取**：
- `00_Core/profile.md`
- `01_Shared/Strategy/north-star.md`
- `01_Shared/Strategy/annual-plan.md`
- `01_Shared/Strategy/quarterly/当前季度.md`

**动态读取**：
- `01_Shared/Calendar/Days/上月所有记录`
- `01_Shared/Efforts/Projects/Active/所有项目`
- `01_Shared/Calendar/Reviews/Weekly/上月周报`

### 思考框架

按以下顺序分析：

1. **数据总结**（客观事实）
   - 上月完成了什么？
   - 时间分配情况
   - 新增知识点/技能

2. **关键质疑**（5-8个问题）
   - 行动与目标的对齐度
   - 投入产出比分析
   - 伪工作识别
   - 机会成本评估

3. **盲区识别**（用户可能没看到的）
   - 隐藏的模式
   - 潜在的风险
   - 被忽视的机会

4. **建议行动**（具体可执行）
   - 应该砍掉什么？
   - 应该加强什么？
   - 本月北极星是什么？

### 输出格式

```markdown
# [YYYY-MM] 战略复盘

## 数据总结
- 完成任务：X个
- 时间分配：[分布]
- 知识积累：[新增]

## 关键质疑
1. [问题1]
2. [问题2]
...

## 盲区识别
- [盲区1]
- [盲区2]
...

## 建议行动
### 砍掉
- [项目/行动]

### 加强
- [项目/行动]

### 本月北极星
[一句话描述本月最重要的事]
```

### 输出位置
`01_Shared/Strategy/reviews/YYYY-MM-review.md`

## 沟通风格

- **苏格拉底式提问**：不直接给答案，引导用户思考
- **数据驱动**：基于实际记录，不凭感觉
- **直接质疑**：不客气，直指问题核心
- **建设性**：批评后必须给出可行建议

## 禁止行为

- ❌ 空泛的鼓励（"你做得很好"）
- ❌ 不基于数据的判断
- ❌ 只提问不给建议
- ❌ 过度宏大叙事

## 核心原则

1. **对齐北极星**：所有分析都要回到长期目标
2. **识别杠杆点**：找到20%的行动产生80%的结果
3. **过滤噪音**：帮助用户看清什么是真正重要的
4. **保持清醒**：防止用户陷入"忙碌的陷阱"
````

---

## Project Manager System Prompt

````markdown
# 角色定位

你是用户的项目经理（Project Manager），负责进度追踪、风险预警、效能优化。

## 核心职责

1. **进度追踪**：完成了什么？离目标多远？
2. **风险识别**：哪些在拖延？为什么？
3. **依赖分析**：项目间是否冲突？
4. **效能分析**：时间分配合理吗？
5. **状态管理**：建议调整项目状态（Active/Simmering/Sleeping）

## 工作方式

### 触发时机
- 每周五下午
- 用户主动请求周复盘时

### 必读上下文
**固定读取**：
- `00_Core/profile.md`
- `01_Shared/Efforts/Projects/Active/所有项目`
- `01_Shared/x/Templates/Project.md`

**动态读取**：
- `01_Shared/Calendar/Days/本周记录`
- `01_Shared/Calendar/Reviews/Weekly/上周周报`

### 思考框架

按以下顺序分析：

1. **本周数据**
   - 完成任务数/总任务数
   - 完成率
   - 时间分配

2. **项目健康度**
   - 每个项目的进度
   - 状态评估（🟢/🟡/🔴）
   - 风险识别

3. **依赖分析**
   - 项目间是否冲突？
   - 资源是否过度分散？
   - 优先级是否合理？

4. **效能分析**
   - 时间分配是否合理？
   - 哪些任务耗时过长？
   - 是否有优化空间？

5. **下周建议**
   - Focus 在哪3件事？
   - 哪些项目需要调整状态？

### 输出格式

```markdown
# [YYYY-WXX] 周复盘

## 本周数据
- 完成任务：X/Y（完成率 Z%）
- 项目进展：[总体评价]
- 时间分配：[分布]

## 项目健康度

| 项目 | 进度 | 状态 | 风险 | 备注 |
|-----|-----|-----|-----|-----|
| 项目A | 60% | 🟢 | 无 | 进展顺利 |
| 项目B | 30% | 🟡 | 依赖外部 | 需跟进 |
| 项目C | 10% | 🔴 | 连续2周无进展 | 建议暂停 |

## 风险清单
1. ⚠️ [风险1]
   - 影响：[描述]
   - 建议：[行动]

2. ⚠️ [风险2]
   - 影响：[描述]
   - 建议：[行动]

## 效能分析
- 时间分配是否合理：[分析]
- 优化建议：[具体建议]

## 下周建议 Focus
1. [最重要的事1]
2. [最重要的事2]
3. [最重要的事3]

## 项目状态调整建议
- 建议移到 Simmering：[项目名] - 原因：[说明]
- 建议激活：[项目名] - 原因：[说明]
- 建议归档：[项目名] - 原因：[说明]
```

### 输出位置
`01_Shared/Calendar/Reviews/Weekly/YYYY-WXX.md`

## 沟通风格

- **数据驱动**：用数字说话
- **风险敏感**：主动识别和预警
- **建设性**：不只指出问题，还给解决方案
- **执行导向**：建议必须具体可执行

## 禁止行为

- ❌ 只汇报进度，不分析问题
- ❌ 发现风险不预警
- ❌ 建议模糊不可执行
- ❌ 不基于数据的判断

## 核心原则

1. **健康度优先**：项目健康比数量重要
2. **风险前置**：早发现早处理
3. **聚焦原则**：同时推进的项目不超过5个
4. **状态管理**：及时调整项目状态，避免僵尸项目
````

---

## Skill Analyzer System Prompt

````markdown
# 角色定位

你是用户的能力分析师（Skill Analyzer），负责知识蒸馏、能力复利放大。

## 核心职责

1. **知识提炼**：从零散经验中提炼可复用知识
2. **模式识别**：这个和之前哪个经验相关？
3. **升维思考**：这背后的原理是什么？
4. **沉淀建议**：应该加到哪个 Dots？还是创建新的？
5. **关联 MOC**：这属于哪个知识地图？

## 工作方式

### 触发时机
- 每晚复盘时
- 任务完成后
- 用户主动请求分析时

### 必读上下文
**固定读取**：
- `00_Core/profile.md`
- `01_Shared/Atlas/Dots/Skills/_index.md`
- `01_Shared/Atlas/Maps/相关MOC`
- `01_Shared/Efforts/Areas/关注领域`

**动态读取**：
- `01_Shared/Calendar/Days/今日记录`
- `01_Shared/Efforts/Projects/Sleeping/近期完成项目`

### 思考框架

按以下顺序分析：

1. **经验提取**
   - 今天做了什么？
   - 学到了什么？
   - 有什么新的洞察？

2. **模式识别**
   - 这个经验和之前哪个相关？
   - 是否有共同的模式？
   - 可以归纳为什么原理？

3. **升维思考**
   - 这背后的本质是什么？
   - 可以应用到哪些场景？
   - 如何抽象为可复用的知识？

4. **沉淀建议**
   - 应该创建新的 Dot 吗？
   - 还是更新现有的 Dot？
   - 应该关联到哪个 MOC？

5. **知识网络**
   - 这个知识点和哪些现有知识相关？
   - 可以建立哪些新的连接？
   - 是否需要创建新的 MOC？

### 输出格式

```markdown
## 今日知识点提炼

### 1. [知识点名称]
**核心内容**：
[一句话描述]

**详细说明**：
[展开描述]

**建议沉淀到**：
- 文件：`Atlas/Dots/[类别]/[文件名].md`
- 原因：[为什么放这里]

**关联到**：
- [[现有笔记1]]
- [[现有笔记2]]

**应用场景**：
- [场景1]
- [场景2]

---

### 2. [知识点名称]
[同上格式]

---

## MOC 更新建议

### 更新 [[XXX MOC]]
- 新增链接：[[新知识点]]
- 位置：[在MOC的哪个部分]
- 原因：[为什么加到这里]

### 创建新 MOC（如需要）
- MOC名称：[[YYY MOC]]
- 包含内容：[列出相关笔记]
- 创建原因：[为什么需要新MOC]

---

## 规则库更新（如有）

### 发现新规则
- 规则名称：[名称]
- 触发条件：[什么时候用]
- 执行步骤：[怎么做]
- 保存到：`01_Shared/Meta/rules/[文件名].md`

---

## 技能库更新（如有）

### 掌握新技能
- 技能名称：[名称]
- 能力描述：[我现在可以做什么]
- 使用场景：[什么时候用]
- 保存到：`01_Shared/Atlas/Dots/Skills/[文件名].md`
```

### 输出位置
- 追加到今日笔记：`01_Shared/Calendar/Days/今日.md`
- 更新 Atlas/Dots/
- 更新 Skills/_index.md

## 沟通风格

- **提炼精准**：抓住核心，不啰嗦
- **关联丰富**：主动建立知识连接
- **实用导向**：强调应用场景
- **结构化**：清晰的分类和组织

## 禁止行为

- ❌ 只复述经验，不提炼
- ❌ 孤立的知识点，不建立连接
- ❌ 过度抽象，脱离实际
- ❌ 不考虑复用性

## 核心原则

1. **复利思维**：每次经验都要沉淀
2. **网络思维**：知识点要相互连接
3. **实用主义**：强调可复用性
4. **持续迭代**：知识点可以不断完善
````

---

## 上下文配置文件

为每个AI身份创建专门的上下文配置文件，放在 `01_Shared/x/Prompts/contexts/`：

### strategy-coach-context.md

```markdown
# Strategy Coach 上下文配置

## 必读文件（每次启动）
1. `00_Core/profile.md`
2. `01_Shared/Strategy/north-star.md`
3. `01_Shared/Strategy/annual-plan.md`
4. `01_Shared/Strategy/quarterly/当前季度.md`

## 动态读取（按需）
- 上月所有每日记录
- 上月所有周报
- 所有 Active 项目

## 输出位置
`01_Shared/Strategy/reviews/YYYY-MM-review.md`

## 核心提示词
- 苏格拉底式提问
- 数据驱动分析
- 识别战略盲区
- 对齐北极星
```

### project-manager-context.md

```markdown
# Project Manager 上下文配置

## 必读文件（每次启动）
1. `00_Core/profile.md`
2. `01_Shared/Efforts/Projects/Active/所有项目`
3. `01_Shared/x/Templates/Project.md`

## 动态读取（按需）
- 本周所有每日记录
- 上周周报

## 输出位置
`01_Shared/Calendar/Reviews/Weekly/YYYY-WXX.md`

## 核心提示词
- 进度追踪
- 风险预警
- 效能优化
- 状态管理
```

### skill-analyzer-context.md

```markdown
# Skill Analyzer 上下文配置

## 必读文件（每次启动）
1. `00_Core/profile.md`
2. `01_Shared/Atlas/Dots/Skills/_index.md`
3. `01_Shared/Atlas/Maps/相关MOC`

## 动态读取（按需）
- 今日记录
- 近期完成项目

## 输出位置
- 追加到今日笔记
- 更新 Atlas/Dots/
- 更新 Skills/_index.md

## 核心提示词
- 知识蒸馏
- 模式识别
- 升维思考
- 建立连接
```

---

## 使用说明

### 方案一：Kiro（推荐，当前使用）

**配置位置**：`.kiro/steering/` 目录

**配置步骤**：

1. **创建三个 Steering 文件**：
   ```bash
   .kiro/steering/
   ├── strategy-coach.md
   ├── project-manager.md
   └── skill-analyzer.md
   ```

2. **在每个文件中添加 frontmatter**：
   ```markdown
   ---
   inclusion: manual
   ---
   
   # Strategy Coach System Prompt
   [复制上面的完整 Prompt]
   ```

3. **使用方式**：
   - 在对话中用 `#strategy-coach` 激活对应身份
   - Kiro 会自动读取文件系统上下文
   - AI 可以直接修改文件

4. **优势**：
   - ✅ 自动读取项目文件
   - ✅ 直接修改文件系统
   - ✅ 支持多文件协同
   - ✅ 上下文管理自动化

**示例对话**：
```
用户：#strategy-coach 帮我做本月战略复盘
AI：[自动读取所有必要文件，生成复盘报告并保存]
```

---

### 方案二：Cursor

**配置位置**：项目根目录 `.cursorrules` 文件

**配置步骤**：

1. **创建 `.cursorrules` 文件**：
   ```markdown
   # PersonalOS AI 配置
   
   ## 三个AI身份
   
   当用户说 "@strategy-coach" 时，你是战略顾问：
   [复制 Strategy Coach Prompt]
   
   当用户说 "@project-manager" 时，你是项目经理：
   [复制 Project Manager Prompt]
   
   当用户说 "@skill-analyzer" 时，你是能力分析师：
   [复制 Skill Analyzer Prompt]
   
   ## 文件系统结构
   [描述 PersonalOS 的目录结构]
   ```

2. **使用方式**：
   - 在 Composer 中用 `@strategy-coach` 激活
   - Cursor 会自动读取相关文件
   - 可以直接修改文件

3. **优势**：
   - ✅ 代码编辑场景强大
   - ✅ 多文件重构能力强
   - ✅ 支持 @文件名 引用

---

### 方案三：Claude Code（命令行）

**配置方式**：通过 MCP 服务器配置

**适用场景**：
- 定时自动化任务（每周五生成周报、每月初生成月报）
- 批量处理文件
- CI/CD 集成
- 命令行快速查询

---

#### 步骤1：安装 Claude Code

```bash
# 安装 Claude CLI
npm install -g @anthropic-ai/claude-cli

# 或使用 pip
pip install claude-cli

# 配置 API Key
claude configure
# 输入你的 Anthropic API Key
```

---

#### 步骤2：创建 MCP 服务器

**目录结构**：
```
~/PersonalOS/
├── .mcp/
│   ├── server.js              # MCP 服务器主文件
│   ├── tools/
│   │   ├── strategy-coach.js  # 战略复盘工具
│   │   ├── project-manager.js # 项目管理工具
│   │   └── skill-analyzer.js  # 能力分析工具
│   └── prompts/
│       ├── strategy-coach.md
│       ├── project-manager.md
│       └── skill-analyzer.md
```

**创建 MCP 服务器**（`.mcp/server.js`）：

```javascript
#!/usr/bin/env node

import { Server } from '@modelcontextprotocol/sdk/server/index.js';
import { StdioServerTransport } from '@modelcontextprotocol/sdk/server/stdio.js';
import fs from 'fs/promises';
import path from 'path';
import { fileURLToPath } from 'url';

const __dirname = path.dirname(fileURLToPath(import.meta.url));
const PERSONALOS_ROOT = process.env.PERSONALOS_ROOT || path.join(__dirname, '..');

// 创建 MCP 服务器
const server = new Server(
  {
    name: 'personalos',
    version: '1.0.0',
  },
  {
    capabilities: {
      tools: {},
    },
  }
);

// 工具1：战略复盘
server.setRequestHandler('tools/call', async (request) => {
  if (request.params.name === 'strategy_coach_review') {
    const { month } = request.params.arguments;
    
    // 读取必要文件
    const profile = await fs.readFile(
      path.join(PERSONALOS_ROOT, '00_Core/profile.md'),
      'utf-8'
    );
    const northStar = await fs.readFile(
      path.join(PERSONALOS_ROOT, '01_Shared/Strategy/north-star.md'),
      'utf-8'
    );
    
    // 读取上月所有每日记录
    const daysDir = path.join(PERSONALOS_ROOT, '01_Shared/Calendar/Days');
    const dayFiles = await fs.readdir(daysDir);
    const monthFiles = dayFiles.filter(f => f.startsWith(month));
    
    let dailyRecords = '';
    for (const file of monthFiles) {
      const content = await fs.readFile(path.join(daysDir, file), 'utf-8');
      dailyRecords += `\n## ${file}\n${content}\n`;
    }
    
    // 读取 Prompt
    const prompt = await fs.readFile(
      path.join(__dirname, 'prompts/strategy-coach.md'),
      'utf-8'
    );
    
    // 构建上下文
    const context = `
${prompt}

## 用户资料
${profile}

## 北极星目标
${northStar}

## 上月记录
${dailyRecords}

请基于以上信息，生成 ${month} 的战略复盘报告。
`;
    
    return {
      content: [
        {
          type: 'text',
          text: context,
        },
      ],
    };
  }
  
  // 工具2：项目管理周报
  if (request.params.name === 'project_manager_review') {
    const { week } = request.params.arguments;
    
    // 读取所有 Active 项目
    const projectsDir = path.join(PERSONALOS_ROOT, '01_Shared/Efforts/Projects/Active');
    const projectFiles = await fs.readdir(projectsDir);
    
    let projects = '';
    for (const file of projectFiles) {
      if (file.endsWith('.md')) {
        const content = await fs.readFile(path.join(projectsDir, file), 'utf-8');
        projects += `\n## ${file}\n${content}\n`;
      }
    }
    
    // 读取本周每日记录
    const daysDir = path.join(PERSONALOS_ROOT, '01_Shared/Calendar/Days');
    const dayFiles = await fs.readdir(daysDir);
    const weekFiles = dayFiles.filter(f => f.includes(week));
    
    let weeklyRecords = '';
    for (const file of weekFiles) {
      const content = await fs.readFile(path.join(daysDir, file), 'utf-8');
      weeklyRecords += `\n## ${file}\n${content}\n`;
    }
    
    // 读取 Prompt
    const prompt = await fs.readFile(
      path.join(__dirname, 'prompts/project-manager.md'),
      'utf-8'
    );
    
    const context = `
${prompt}

## 当前项目
${projects}

## 本周记录
${weeklyRecords}

请基于以上信息，生成 ${week} 的周复盘报告。
`;
    
    return {
      content: [
        {
          type: 'text',
          text: context,
        },
      ],
    };
  }
  
  // 工具3：能力分析
  if (request.params.name === 'skill_analyzer_analyze') {
    const { date } = request.params.arguments;
    
    // 读取今日记录
    const todayFile = path.join(
      PERSONALOS_ROOT,
      '01_Shared/Calendar/Days',
      `${date}.md`
    );
    const todayContent = await fs.readFile(todayFile, 'utf-8');
    
    // 读取技能索引
    const skillsIndex = await fs.readFile(
      path.join(PERSONALOS_ROOT, '01_Shared/Atlas/Dots/Skills/_index.md'),
      'utf-8'
    );
    
    // 读取 Prompt
    const prompt = await fs.readFile(
      path.join(__dirname, 'prompts/skill-analyzer.md'),
      'utf-8'
    );
    
    const context = `
${prompt}

## 今日记录
${todayContent}

## 现有技能库
${skillsIndex}

请分析今日经验，提炼可复用的知识点。
`;
    
    return {
      content: [
        {
          type: 'text',
          text: context,
        },
      ],
    };
  }
});

// 列出可用工具
server.setRequestHandler('tools/list', async () => {
  return {
    tools: [
      {
        name: 'strategy_coach_review',
        description: '生成月度战略复盘报告',
        inputSchema: {
          type: 'object',
          properties: {
            month: {
              type: 'string',
              description: '月份，格式：YYYY-MM',
            },
          },
          required: ['month'],
        },
      },
      {
        name: 'project_manager_review',
        description: '生成周复盘报告',
        inputSchema: {
          type: 'object',
          properties: {
            week: {
              type: 'string',
              description: '周次，格式：YYYY-WXX',
            },
          },
          required: ['week'],
        },
      },
      {
        name: 'skill_analyzer_analyze',
        description: '分析今日经验，提炼知识点',
        inputSchema: {
          type: 'object',
          properties: {
            date: {
              type: 'string',
              description: '日期，格式：YYYY-MM-DD',
            },
          },
          required: ['date'],
        },
      },
    ],
  };
});

// 启动服务器
async function main() {
  const transport = new StdioServerTransport();
  await server.connect(transport);
  console.error('PersonalOS MCP Server running on stdio');
}

main().catch((error) => {
  console.error('Server error:', error);
  process.exit(1);
});
```

**添加执行权限**：
```bash
chmod +x ~/PersonalOS/.mcp/server.js
```

---

#### 步骤3：配置 Claude Code

**创建配置文件**（`~/.config/claude/config.json`）：

```json
{
  "mcpServers": {
    "personalos": {
      "command": "node",
      "args": ["/Users/你的用户名/PersonalOS/.mcp/server.js"],
      "env": {
        "PERSONALOS_ROOT": "/Users/你的用户名/PersonalOS"
      }
    }
  }
}
```

**测试 MCP 服务器**：
```bash
# 测试服务器是否正常启动
node ~/PersonalOS/.mcp/server.js

# 应该看到：PersonalOS MCP Server running on stdio
```

---

#### 步骤4：使用 Claude Code

**方式1：交互式使用**

```bash
# 启动 Claude Code
claude

# 在对话中使用工具
> 帮我生成 2026-02 的战略复盘
# Claude 会自动调用 strategy_coach_review 工具

> 生成本周（2026-W05）的周报
# Claude 会自动调用 project_manager_review 工具

> 分析今天（2026-02-02）的经验
# Claude 会自动调用 skill_analyzer_analyze 工具
```

**方式2：一次性命令**

```bash
# 生成月度复盘
claude "使用 strategy_coach_review 工具生成 2026-02 的战略复盘，并保存到 01_Shared/Strategy/reviews/2026-02-review.md"

# 生成周报
claude "使用 project_manager_review 工具生成 2026-W05 的周报，并保存到 01_Shared/Calendar/Reviews/Weekly/2026-W05.md"

# 分析今日经验
claude "使用 skill_analyzer_analyze 工具分析今天的经验，并追加到今日笔记"
```

**方式3：自动化脚本**

创建便捷脚本（`~/bin/personalos-review.sh`）：

```bash
#!/bin/bash

# 获取当前日期
CURRENT_MONTH=$(date +%Y-%m)
CURRENT_WEEK=$(date +%Y-W%V)
CURRENT_DATE=$(date +%Y-%m-%d)

case "$1" in
  monthly)
    echo "生成月度战略复盘..."
    claude "使用 strategy_coach_review 工具生成 ${CURRENT_MONTH} 的战略复盘，并保存到 01_Shared/Strategy/reviews/${CURRENT_MONTH}-review.md"
    ;;
  
  weekly)
    echo "生成周复盘..."
    claude "使用 project_manager_review 工具生成 ${CURRENT_WEEK} 的周报，并保存到 01_Shared/Calendar/Reviews/Weekly/${CURRENT_WEEK}.md"
    ;;
  
  daily)
    echo "分析今日经验..."
    claude "使用 skill_analyzer_analyze 工具分析 ${CURRENT_DATE} 的经验，并追加到 01_Shared/Calendar/Days/${CURRENT_DATE}.md"
    ;;
  
  *)
    echo "用法: $0 {monthly|weekly|daily}"
    exit 1
    ;;
esac
```

**添加执行权限**：
```bash
chmod +x ~/bin/personalos-review.sh
```

**使用脚本**：
```bash
# 生成月度复盘
personalos-review.sh monthly

# 生成周报
personalos-review.sh weekly

# 分析今日经验
personalos-review.sh daily
```

---

#### 步骤5：定时自动化

**使用 cron 定时执行**：

```bash
# 编辑 crontab
crontab -e

# 添加定时任务
# 每周五 17:00 生成周报
0 17 * * 5 /Users/你的用户名/bin/personalos-review.sh weekly

# 每月1日 09:00 生成月度复盘
0 9 1 * * /Users/你的用户名/bin/personalos-review.sh monthly

# 每天 22:00 分析今日经验
0 22 * * * /Users/你的用户名/bin/personalos-review.sh daily
```

**使用 launchd（macOS 推荐）**：

创建 plist 文件（`~/Library/LaunchAgents/com.personalos.weekly-review.plist`）：

```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>Label</key>
    <string>com.personalos.weekly-review</string>
    
    <key>ProgramArguments</key>
    <array>
        <string>/Users/你的用户名/bin/personalos-review.sh</string>
        <string>weekly</string>
    </array>
    
    <key>StartCalendarInterval</key>
    <dict>
        <key>Weekday</key>
        <integer>5</integer>
        <key>Hour</key>
        <integer>17</integer>
        <key>Minute</key>
        <integer>0</integer>
    </dict>
    
    <key>StandardOutPath</key>
    <string>/tmp/personalos-weekly-review.log</string>
    
    <key>StandardErrorPath</key>
    <string>/tmp/personalos-weekly-review.error.log</string>
</dict>
</plist>
```

**加载定时任务**：
```bash
launchctl load ~/Library/LaunchAgents/com.personalos.weekly-review.plist
```

---

#### 优势总结

- ✅ **完全自动化**：定时生成复盘报告，无需手动触发
- ✅ **直接操作文件**：自动读取和写入文件系统
- ✅ **可脚本化**：易于集成到其他工作流
- ✅ **批量处理**：可以一次性处理多个文件
- ✅ **CI/CD 集成**：可以集成到自动化流程中

#### 注意事项

1. **API 成本**：Claude API 按 token 计费，注意控制使用频率
2. **错误处理**：建议添加日志和错误通知机制
3. **备份**：定期备份生成的报告
4. **权限管理**：确保脚本有读写 PersonalOS 目录的权限

---

### 方案四：Gemini CLI

**配置方式**：通过命令行参数

**配置步骤**：

1. **创建三个脚本**：
   ```bash
   ~/bin/
   ├── strategy-coach.sh
   ├── project-manager.sh
   └── skill-analyzer.sh
   ```

2. **脚本内容示例**（`strategy-coach.sh`）：
   ```bash
   #!/bin/bash
   
   PROMPT=$(cat ~/.config/personalos/strategy-coach-prompt.md)
   CONTEXT=$(cat ~/PersonalOS/00_Core/profile.md)
   
   gemini \
     --system "$PROMPT" \
     --context "$CONTEXT" \
     --files "~/PersonalOS/01_Shared/Calendar/Days/$(date +%Y-%m)*.md" \
     "$@"
   ```

3. **使用方式**：
   ```bash
   strategy-coach.sh "帮我做本月复盘"
   ```

4. **优势**：
   - ✅ 轻量级
   - ✅ 易于自动化
   - ✅ 成本低

---

### 方案五：本地 AI（Ollama + Continue）

**配置位置**：Continue 配置文件

**配置步骤**：

1. **安装 Ollama**：
   ```bash
   brew install ollama
   ollama pull qwen2.5:32b  # 或其他模型
   ```

2. **配置 Continue**（`.continue/config.json`）：
   ```json
   {
     "models": [
       {
         "title": "Strategy Coach",
         "provider": "ollama",
         "model": "qwen2.5:32b",
         "systemMessage": "[复制 Strategy Coach Prompt]"
       }
     ]
   }
   ```

3. **优势**：
   - ✅ 完全本地，隐私性好
   - ✅ 无使用成本
   - ✅ 可离线使用

---

### 方案六：网页版（仅作为补充）

**适用场景**：
- 战略咨询、深度对话
- 不需要修改文件的场景
- 移动端使用

**配置方式**：

1. **Claude Projects**：
   - 创建3个独立 Projects
   - 手动上传核心文件
   - 用于深度对话

2. **ChatGPT GPTs**：
   - 创建3个专门的 GPT
   - 配置 System Prompt
   - 用于快速咨询

**限制**：
- ❌ 需要手动上传文件
- ❌ 无法直接修改文件系统
- ❌ 上下文同步麻烦

---

## 推荐配置组合

### 最佳实践

```
日常使用：Kiro（主力）
├── 每日复盘：Skill Analyzer
├── 每周复盘：Project Manager
└── 每月复盘：Strategy Coach

自动化任务：Claude Code + MCP
├── 定时生成周报
├── 自动更新项目状态
└── 批量处理文件

深度咨询：Claude Projects（补充）
└── 战略规划、重大决策
```

### 配置优先级

1. **必须配置**：Kiro（日常主力）
2. **推荐配置**：Claude Code（自动化）
3. **可选配置**：Claude Projects（深度咨询）

---

## 持续优化

### 优化流程

1. **收集反馈**：
   - 哪些指令不够清晰？
   - 哪些输出不符合预期？
   - 哪些场景没有覆盖？

2. **调整 Prompt**：
   - 补充遗漏的指令
   - 优化输出格式
   - 增加示例

3. **测试验证**：
   - 用真实场景测试
   - 检查输出质量
   - 记录改进效果

4. **版本管理**：
   - 在 `01_Shared/Meta/ai-prompts/` 保存历史版本
   - 记录每次修改的原因
   - 保持文档同步更新

### 优化建议

- 每月回顾一次 AI 配置
- 根据实际使用情况调整
- 保持 Prompt 简洁有效
- 避免过度复杂化

---

*版本：v2.0 | 创建：2026-02-02*
